name: Prevent File Deletion

on:
  push:
    branches:
      - main

jobs:
  prevent-deletion:
    runs-on: ubuntu-latest
    steps:
      - name: Check for deleted files
        run: |
          git diff HEAD~1 HEAD | grep '+++ b/s' && exit 1 || exit 0
